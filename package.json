{
  "name": "figma-mcp-server",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "setup": "cd plugin && npm install && npm run build && cd ../mcp && npm install && npm run build",
    "start": "cd mcp && npm run serve"
  }
}
